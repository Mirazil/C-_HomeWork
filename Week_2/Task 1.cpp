#include <iostream>
#include <math.h>

using namespace std;

int main()
{
    //Приклад 2
    float x, a, c, Y;
    
    if(x <= 0)
        Y = 2 * pow(x, 2) + a;
    else if(x > 0)
        Y = (x + 3 * a) * c;
    
    return 0;
}